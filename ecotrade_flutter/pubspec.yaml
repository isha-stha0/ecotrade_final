name: ecotrade
description: EcoTrade - Smart Scrap to Product Platform
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  fl_chart: ^0.65.0
  intl: ^0.18.1
  image_picker: ^1.0.0
  flutter_map: ^6.0.0
  latlong2: ^0.9.0
  url_launcher: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/ecotrade_logo.jpg
